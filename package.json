{
  "name": "interstitia",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "eslint": "^8.0.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-jest": "^27.0.0",
    "jest": "^29.0.0",
    "prettier": "^3.0.0"
  },
  "scripts": {
    "test": "jest",
    "lint": "eslint contents/code/*.js --ignore-pattern contents/code/main.js",
    "format": "prettier --write contents/code/*.js --ignore-path .prettierignore"
  }
}
